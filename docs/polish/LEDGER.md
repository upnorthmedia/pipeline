# Polish ledger

The worklist for the "make Jena AI work correctly and feel well made" run. One small
verified committed change per iteration, working the topmost unchecked item. Sections are in
priority order and do not get reordered: an earlier section done properly beats all of it
half done.

Evidence for every item goes in `docs/polish/evidence/<section>.md` under a `## <item-id>`
heading. This file carries the link and nothing else, because it is re-read every iteration.

Splitting: an item may be split two levels deep at most (`2` -> `2a` -> `2a-i`). Below that,
finish it in one iteration however long it takes, or mark it `[blocked]` with a reason and
move on. A split is not an iteration on its own: split and complete the first child together.

---

## P0. Nothing is failing, and CI proves it

- [x] **P0.1** The 6 failing tests in `web/src/components/__tests__/image-preview.test.tsx`.
      [evidence](evidence/p0.md#p01)
- [x] **P0.2** `src/mastra/workflows/scaffold-check.test.ts` fails deterministically whenever a
      Mastra worker is running: it uses the default Redis database and topic while
      `worker-process.test.ts` (db 9), `web-restart.test.ts` (db 10) and `crash-probe.test.ts`
      (db 11) isolate. Isolate it. [evidence](evidence/p0.md#p02)
- [ ] **P0.3** Make the suite deterministic. Suites borrow the global `settings.api_keys` row,
      swap `globalThis.fetch`, collide on post ids and write real files into the repo. Prove it
      with three identical `pnpm test` runs, worker running and not running.
  - [x] **P0.3a** The global `settings.api_keys` row nine files borrow: the snapshot lives in
        the database, so a killed run cannot lose the developer's keys and the next run repairs
        the row instead of adopting the placeholder. [evidence](evidence/p0.md#p03a)
  - [x] **P0.3b** `pipeline-events.test.ts > carries Python's log payload and nothing else`:
        the gap is in the recording subscriber, not the Redis Streams fan-out. The transport
        invokes callbacks in stream order but does not await them, so the test's own row read
        decided the order of `received`. Covers P2.37, P2.42, P2.48 and P2.69.
        [evidence](evidence/p0.md#p03b)
  - [x] **P0.3c** The suites that swap `globalThis.fetch`. Not P2.8's suspects: those eight
        restore correctly, and the three files that did leak were `api.test.ts`,
        `images-generate.test.ts` and `stage-models-to-provider.test.tsx`. All eleven now go
        through `src/test/swapped-fetch.ts`, and a guard in `setup.ts` fails any test that
        finds the transport left swapped. [evidence](evidence/p0.md#p03c)
  - [ ] **P0.3d** Post ids colliding across files, which vitest runs in parallel against one
        database (P2.53). One registry, or a per-file prefix that is enforced.
  - [ ] **P0.3e** The proof: three `pnpm test` runs with identical summaries, worker running and
        worker not running, with all six summaries in the evidence.
- [ ] **P0.4** The suite must not dirty the working tree: tests write to a temp dir, and the 39
      committed image files under `media/test-123/` come out.
- [ ] **P0.5** `pnpm test:e2e` is stale and fails. Repair the Playwright suite against the
      current UI.
- [ ] **P0.6** CI. Add `.github/workflows` running tsc, lint, test, build and e2e on push and
      PR with Postgres and Redis services. Green on a real run, URL in the evidence. This is
      the acceptance criterion for all of P0.

## P1. The three things a user sees immediately

- [ ] **P1.1** The logo is broken. `web/src/components/sidebar.tsx:60` renders
      `<Image src="/icon.svg">` and `web/public/icon.svg` is a 49KB SVG whose content is an
      HTML `<foreignObject>`, which browsers do not render through `<img>`. Produce a real
      vector mark used consistently for the sidebar, the favicon and the PWA manifest icons.
      Verify by screenshot.
- [ ] **P1.2** Post images are broken. Two media directories hold 0 files while their manifests
      claim 4 and 5 images, 4 manifest entries carry an `error`, and `stage_status.images`
      still reads `complete`. Both causes get fixed: the stage reporting success when every
      image failed, and the UI rendering entries as `<img>` without checking for an error or a
      missing file. A failed image reads as a failure with its reason and a retry. Includes
      WordPress publishing uploading no images (P2.63) and generated images being unreachable
      from the `web` service on Railway (P2.76).
- [ ] **P1.3** Publishing is incomprehensible. One obvious place to configure and test a
      connection, a visible connection status, output formats that disable themselves with an
      explanation when their target is not configured, and a publish action that reports what
      happened. Fix the broken endpoints underneath it (P2.57, P2.58, P2.59, P2.61, P2.63).

## P2. Clear the backlog in `todo.md`

Worked top to bottom against `todo.md`, one numbered entry per finding in that file's order.
For each: fix it, or delete it with a one-line reason in the evidence file if it is stale or
wrong. Every `[confirmed]` item affecting correctness, data integrity, security or anything a
user can see gets fixed. Every `[investigate]` item gets verified before it is acted on, and
demoted or deleted when it does not reproduce.

High value, done first within P2: P2.51 (`delete_link()` ownership check), P2.87
(`POST /api/profiles` accepting any string as a website URL), P2.83 (`GET /api/posts`
answering 500 with an empty body), P2.84 (`/posts/[id]` naming two of the six stages twice),
P2.31 (duplicate silently dropping `article_type`), P2.77 (migrations never running on
deploy), P2.71 (Perplexity's deprecated Sonar Chat Completions endpoint).

- [x] **P2.1** `[confirmed]` `src/mastra/workflows/scaffold-check.test.ts` fails whenever a real Mastra worker is running against the same Redis, because it uses the default ... Fixed by P0.2; entry removed from `todo.md`. [evidence](evidence/p0.md#p02)
- [ ] **P2.2** `[confirmed]` The `images` stage's featured-image handling never fires on real manifests. `images.py` tests `image_spec.get("placement") == "featured"`, but ...
- [ ] **P2.3** `[confirmed]` The Gemini API key in the main checkout's `.env` has **zero** image quota: every `gemini-3.1-flash-image` call returns 429 `RESOURCE_EXHAUSTED` ...
- [ ] **P2.4** `[investigate]` `ClaudeClient.chat` sends `thinking={"type": "enabled", "budget_tokens": 10000}`, which the installed anthropic SDK now warns is deprecated for ...
- [ ] **P2.5** `[confirmed]` `settings` primary key is `key` alone while `user_id` is only an index, so two users cannot hold different values for the same setting key. ...
- [ ] **P2.6** `[confirmed]` `api/src/models/post.py` disagrees with the Alembic-produced database on two defaults: it declares `output_format` server default `"markdown"` ...
- [ ] **P2.7** `[confirmed]` `next build`'s output file tracing does not know about the two runtime data files under `web/src/mastra/textstat/data/`, which the port reads ...
- [ ] **P2.8** `[investigate]` `src/mastra/agents/{edit,research,write}.test.ts` fail intermittently under a full `pnpm test` run (seen: 4 extra failures, then 1, then 0 ... Both causes it named are now addressed (P0.3a for the settings row, P0.3c for the transport, which measured these eight files as *not* leaking); stays open until P0.3e's six runs. [evidence](evidence/p0.md#p03c)
- [x] **P2.9** `[confirmed]` The agent test files' `beforeAll`/`afterAll` key save-and-restore makes the placeholder key permanent: a run captures whatever is in ... Fixed by P0.3a; entry removed from `todo.md`. [evidence](evidence/p0.md#p03a)
- [ ] **P2.10** `[confirmed]` `_generate_one` gives every featured image the same filename, `featured-<MMDDYY>-<randint(10,99)>.webp`, so two featured entries in one manifest ...
- [ ] **P2.11** `[confirmed]` The featured overrides in `_generate_one` rewrite the local `aspect_ratio` / `image_size` without touching the manifest entry, so a stored entry ...
- [ ] **P2.12** `[confirmed]` An image spec whose `filename` is the empty string writes a dotfile, `<media_dir>/<post_id>/.webp`, and records `/media/<post_id>/.webp`. Hidden ...
- [ ] **P2.13** `[investigate]` Python reads `aspect_ratio`, `image_size` and `filename` out of the manifest with `dict.get(key, default)`, which returns an explicit JSON ...
- [ ] **P2.14** `[confirmed]` A `pnpm test` run writes generated images into the repo's own `media/test-123/` rather than a temp directory, and 41 of them are already ...
- [ ] **P2.15** `[confirmed]` The `posts.stage_settings` column default in the live database is ...
- [ ] **P2.16** `[confirmed]` `EventedRun.resume()` resolves with a stale snapshot. It subscribes to the shared `workflows-finish` topic and the Redis stream still holds the ...
- [ ] **P2.17** `[confirmed]` `web/src/lib/api.ts` types `StageMode` as `"auto"` and `StageStatus` without `"review"`, so the dashboard cannot express a gated stage. Item 4.3 ...
- [ ] **P2.18** `[confirmed]` The `mastra worker` bundle does not inherit the dev machine's module resolution. Two packages that `next build`, `vitest` and `mastra dev` all ...
- [ ] **P2.19** `[investigate]` `mastra worker start` prints `[mastra] Shutting down workers...` twice under a process-group signal, because the CLI and the worker it spawns ...
- [ ] **P2.20** `[investigate]` `src/mastra/workflows/scaffold-check.test.ts` intermittently drains a `run.stream()` that is missing `workflow-step-start` / ... Same root cause as P0.2 (two consumers on the default orchestration topic); needs P0.3's full-suite runs to close. [evidence](evidence/p0.md#p02)
- [ ] **P2.21** `[investigate]` Item 4.5a only covers `SIGKILL`. A Railway redeploy sends `SIGTERM`, which the `mastra worker` entry handles by calling `stopWorkers()`, and ...
- [ ] **P2.22** `[confirmed]` The worker bundle resolves `rules/` from its own output directory, so it needs `RULES_DIR` set explicitly. `prompts.ts` falls back to ...
- [ ] **P2.23** `[confirmed]` The worker bundle also needs `TEXTSTAT_DATA_DIR` set, and unlike the missing rules it is fatal. `textstatDataDir()` falls back to ...
- [ ] **P2.24** `[confirmed]` `web/src/components/__tests__/export-button.test.tsx` writes real files into the repo's `media/test-123/` on every `pnpm -C web test` run, ...
- [ ] **P2.25** `[confirmed]` Test post ids have to be unique across the whole vitest suite, not just within a file: files run in parallel, so two files seeding the same ...
- [ ] **P2.26** `[confirmed]` `settings.key` is the entire primary key, so two users cannot both hold one settings key. `PATCH /api/settings` therefore 500s with a `23505` ...
- [x] **P2.27** `[fixed]` The six agent test files each rewrote and deleted the single global `api_keys` settings row (`writeAnthropicKey` / `clearKeys` in ...
- [ ] **P2.28** `[investigate]` BetterAuth has no `BETTER_AUTH_SECRET` in `.env` or `.env.example`, so it falls back to its built-in default secret and every session cookie in ...
- [ ] **P2.29** `[investigate]` `scaffold-check.test.ts > emits the workflow lifecycle events the trace view will read` failed once in a full `pnpm -C web test` run with ... Same root cause as P0.2 (two consumers on the default orchestration topic); needs P0.3's full-suite runs to close. [evidence](evidence/p0.md#p02)
- [ ] **P2.30** `[confirmed]` `web/src/app/api/profiles/serialize.ts` formats timestamps with a plain `Date.toISOString()`, which always writes exactly three fractional ...
- [ ] **P2.31** `[confirmed]` `POST /api/posts/{post_id}/duplicate` silently drops `article_type` and `additional_info`. `duplicate_post`'s `config_fields` list in ...
- [ ] **P2.32** `[confirmed]` `PostAnalytics.seo_checklist` in `web/src/lib/api.ts` is typed `Record<string, boolean>` but the endpoint has always returned a mixed map: ...
- [ ] **P2.33** `[confirmed]` 39 generated image files under `media/test-123/` are committed to the repo. They are test output: the media-writing tests in the images-stage ...
- [ ] **P2.34** `[confirmed]` `retry_dead_letter()` in `api/src/api/queue.py` looks the post up with an unscoped `session.get(Post, post_id)`, so any authenticated user can ...
- [ ] **P2.35** `[investigate]` Mastra's engine logs step failures to a logger the app cannot configure. `MastraBase`'s constructor gives every primitive its own ...
- [ ] **P2.36** `[investigate]` `src/mastra/workflows/scaffold-check.test.ts > emits the workflow lifecycle events the trace view will read` failed once on a full `vitest run` ... Same root cause as P0.2 (two consumers on the default orchestration topic); needs P0.3's full-suite runs to close. [evidence](evidence/p0.md#p02)
- [x] **P2.37** `[investigate]` `src/mastra/pipeline-events.test.ts > carries Python's log payload and nothing else` failed once on a full `vitest run` during item 5.5e-i, ... Fixed by P0.3b; entry removed from `todo.md`. [evidence](evidence/p0.md#p03b)
- [ ] **P2.38** `[confirmed]` The `cost_usd` on every `stage_complete` execution log entry is priced at Anthropic Opus rates regardless of provider. `api/src/worker.py:244` ...
- [ ] **P2.39** `[confirmed]` The port's stage retries are immediate where Python spaced them 10 seconds apart. `WorkerSettings.retry_delay = 10` (`api/src/worker.py:610`) ...
- [ ] **P2.40** `[investigate]` A step that is resumed from a review gate and then throws loses its `resumeData` on retry, so it re-suspends at the same gate instead of ...
- [x] **P2.41** `[confirmed, fixed 2026-08-22]` `images` stage did not retry at all, where Python's job retry re-ran it like any other stage. Measured under ledger item 5.5c-iii-b-2-b-i ...
- [x] **P2.42** `[confirmed]` `received` in `web/src/mastra/pipeline-events.test.ts` is not in delivery order, so any assertion built on its ordering is unsound. Its ... Fixed by P0.3b; entry removed from `todo.md`. [evidence](evidence/p0.md#p03b)
- [x] **P2.43** `[confirmed, fixed 2026-08-23]` test files shared post id `00000000-0000-4000-8000-0000000055d1`: `web/src/mastra/steps/stage-log.test.ts` and ...
- [x] **P2.44** `[confirmed, fixed 2026-08-23]` in `web/src/mastra/failure-recorder.test.ts` returned as soon as `current_stage` read `failed`, which is a snapshot that can predate the ...
- [ ] **P2.45** `[optimization 2026-08-23]` carries three literal U+2014 em dashes in source (the `em-dash(es)` warning message, the `ACTION REQUIRED` prompt line, and one comment), where ...
- [ ] **P2.46** `[confirmed]` `web/src/mastra/steps/images-manifest.ts` renders the manifest's `error` value into its parse-failure message with `String(...)` where Python's ...
- [ ] **P2.47** `[confirmed]` `web/src/hooks/use-sse.ts` does not listen for `image_generated` or `image_failed`. `NAMED_EVENTS` lists eight names and neither is among them, ...
- [x] **P2.48** `[investigate]` New sighting of the `received`-ordering defect recorded above: `pipeline-events.test.ts > carries Python's log payload and nothing else` failed ... Fixed by P0.3b; entry removed from `todo.md`. [evidence](evidence/p0.md#p03b)
- [ ] **P2.49** `[investigate]` Running `pnpm -C web test` leaves untracked WebP files in the repo's `media/test-123/` directory (six appeared during the 5.5c-iv-d-2 iteration, ...
- [ ] **P2.50** `[optimization]` `GET /api/events/{post_id}` opens one `RedisStreamsPubSub` subscription per connected browser, and each one costs a dedicated Redis connection ...
- [ ] **P2.51** `[confirmed]` `delete_link()` in `api/src/api/links.py` has no ownership check: unlike the other two endpoints in that router it never calls ...
- [ ] **P2.52** `[confirmed]` `cost_analytics()` in `api/src/api/analytics.py` has never run. Its raw SQL puts the `website_profiles` join inside the second `FROM` item ...
- [ ] **P2.53** `[confirmed]` `web/src/app/api/analytics/costs/route.ts` (ledger item 5.8b) reads its two optional query parameters with `url.searchParams.get("profile_id")` ...
- [ ] **P2.54** `[confirmed]` `npx next build` in `web/` prints 15 `[Error [BetterAuthError]: You are using the default secret. Please set BETTER_AUTH_SECRET ...]` lines ...
- [ ] **P2.55** `[confirmed]` `GET /api/analytics/logs` in `api/src/api/analytics.py` answers a 500 for any `since` or `until` that `datetime.fromisoformat()` rejects. The ...
- [ ] **P2.56** `[confirmed]` The whole `/api/analytics/logs` result set is ordered and bounded by a *text* comparison on `log_entry->>'ts'`, in both stacks. That is only ...
- [ ] **P2.57** `[confirmed]` `WordPressClient.__init__` in `api/src/services/wordpress.py` appends its REST paths to the raw `wp_url` after nothing but a trailing-slash ...
- [ ] **P2.58** `[confirmed]` `GET /api/profiles/{profile_id}/wordpress/categories` and `/authors` answer a 500 for every failure the WordPress install reports, because ...
- [ ] **P2.59** `[confirmed]` `GET /api/profiles/{profile_id}/wordpress/test` answers a 500, not `{connected: false}`, when the configured `wp_url` serves valid JSON that is ...
- [ ] **P2.60** `[confirmed]` `src/mastra/workflows/scaffold-check.test.ts > emits the workflow lifecycle events the trace view will read` fails inside a full `pnpm test` run ... Same root cause as P0.2 (two consumers on the default orchestration topic); needs P0.3's full-suite runs to close. [evidence](evidence/p0.md#p02)
- [ ] **P2.61** `[confirmed]` `markdown_to_wp_html` raises `AttributeError: No renderer "'inline_html'"` for any article whose markdown contains an HTML tag mistune's block ...
- [ ] **P2.62** `[confirmed]` The TypeScript port of mistune's `linebreak` and `softbreak` inline patterns in `web/src/mastra/wordpress/wp-html.ts` still spells Python's `\s` ...
- [ ] **P2.63** `[confirmed]` WordPress publishing uploads no images at all in production. The media sweep in `api/src/pipeline/publish.py` keeps a file only when ...
- [ ] **P2.64** `[confirmed]` The local-to-remote image URL rewrite in `api/src/pipeline/publish.py` corrupts a media URL that is a prefix of another one. The loop is `for ...
- [ ] **P2.65** `[investigate]` `src/mastra/workflows/pipeline-completion.test.ts > ... (`Date.parse(recorded)` at line 328) failed once in a full `pnpm test` run during ledger ...
- [ ] **P2.66** `[confirmed]` `pythonJsonDumps` in `web/src/mastra/prompts.ts` does not escape U+007F. Its regex is `[\u0080-\uffff]`, but Python's `ESCAPE_ASCII` is `[^\ ...
- [ ] **P2.67** `[investigate]` The `pg` driver parses a JSONB column with `JSON.parse`, which erases Python's int/float distinction and loses digits past 2^53 before any port ...
- [ ] **P2.68** `[confirmed]` `POST /api/posts/{post_id}/publish` cannot publish a post whose `output_format` is `both`, which is the column's default value. `publish_post()` ...
- [x] **P2.69** `[confirmed]` `web/src/mastra/pipeline-events.test.ts > a run that executes every stage > carries Python's log payload and nothing else` is flaky: it reads ... Fixed by P0.3b; entry removed from `todo.md`. [evidence](evidence/p0.md#p03b)
- [x] **P2.70** `[confirmed]` The vitest suites that borrow the global `settings.api_keys` row can destroy live credentials. `web/src/mastra/api-keys.test.ts` deletes the row ... Fixed by P0.3a; entry removed from `todo.md`. [evidence](evidence/p0.md#p03a)
- [ ] **P2.71** `[confirmed]` Perplexity's Sonar Chat Completions endpoint is deprecated with support ending 2026-09-27, per `docs.perplexity.ai/getting-started/models`. The ...
- [ ] **P2.72** `[confirmed]` The Playwright suite (`pnpm -C web test:e2e`) is stale. A full run during ledger item 7.1b ended `4 passed (7.4m)` with failures listed across ...
- [ ] **P2.73** `[confirmed]` `src/mastra/workflows/scaffold-check.test.ts` fails intermittently under a full `pnpm -C web test` run with `Error: Hook timed out in 60000ms` ... Different symptom (a `beforeAll` timeout, not a short stream) but plausibly the same starved subscription as P0.2; unmeasured, so it needs P0.3's full-suite runs. [evidence](evidence/p0.md#p02)
- [ ] **P2.74** `[confirmed]` A worker process running outside the test suite breaks the suite. While a `jena-worker` container was up against the shared dev Redis, a full ...
- [ ] **P2.75** `[investigate]` `next build` inside the web image prints seven `[Error [BetterAuthError]: You are using the default secret...]` lines plus a matching BetterAuth ...
- [ ] **P2.76** `[confirmed]` Generated images are unreachable from the `web` service on Railway. The `/media` volume is mounted on `worker`, which writes them, but Railway ...
- [ ] **P2.77** `[confirmed]` Nothing runs database migrations on deploy. `deploy.preDeployCommand` is the documented Railway hook, but neither runtime image carries ...
- [ ] **P2.78** `[confirmed]` `scaffold-check.test.ts > emits the workflow lifecycle events the trace view will read` fails under a full parallel `pnpm -C web test` while ... Same root cause as P0.2 (two consumers on the default orchestration topic); needs P0.3's full-suite runs to close. [evidence](evidence/p0.md#p02)
- [ ] **P2.79** `[confirmed]` The objective's literal typecheck gate `pnpm -C web tsc --noEmit` does not run on the installed pnpm (10.26.2): it exits 254 with ...
- [x] **P2.80** `[confirmed]` The 6 failing tests in `web/src/components/__tests__/image-preview.test.tsx`. The same finding as P0.1, removed from `todo.md`. [evidence](evidence/p0.md#p01)
- [ ] **P2.81** `[confirmed]` The app has no theme switch: `web/src/app/layout.tsx:28` hardcodes `<html lang="en" className="dark">` and no `ThemeProvider` is mounted ...
- [ ] **P2.82** `[investigate]` `pnpm -C web dev` serves the repo's own `media/` directory, but the item 7.6 end-to-end run executed in the compose stack and wrote its five ...
- [ ] **P2.83** `[investigate]` `GET /api/posts` answers `500` with an empty body when its database is unreachable (verified by stopping the compose `db` container under a live ...
- [ ] **P2.84** `[confirmed]` `/posts/[id]` names two of the six stages twice: the tab strip in `web/src/app/posts/[id]/page.tsx` labels them "Draft" and "Editing" while the ...
- [ ] **P2.85** `[confirmed]` The `posts.stage_logs` column is write-only dead weight in the ported stack: the only key anything writes is `_error` ...
- [ ] **P2.86** `[confirmed]` `web/e2e/navigation.test.ts:56` asserts a "Pipeline Settings" card on `/posts/new` and no such card exists in `web/src/app/posts/new/page.tsx` ...
- [ ] **P2.87** `[confirmed]` `POST /api/profiles` accepts any string as `website_url`: creating a profile with the literal value `not a url` succeeded during item 8.6's live ...
- [ ] **P2.88** `[confirmed]` `pnpm -C web test` is not deterministic above the known `image-preview.test.tsx` baseline: one extra database-backed test fails per full-suite ...
- [ ] **P2.89** `[confirmed]` The global `stage_models` row is borrowed the way `api_keys` used to be: three files hold the pre-borrow value in memory and write it back in `afterAll`, so a killed run loses it. Same shape P0.3a fixed for `api_keys`; move it onto `web/src/test/borrowed-rows.ts`, which first needs to carry `updated_at`.

## P3. UI and UX

Not a repaint: the shadcn and Tailwind v4 foundation stays. Simple, fast, legible, and
organised so the next action is obvious. Dark mode and the accessibility checklist are
explicitly out of scope.

- [ ] **P3.1** Information architecture. Settings, profiles and per-post configuration overlap
      and nothing says which owns what. Decide, move things, write the reasoning in the
      evidence file. Publishing configuration drives this.
- [ ] **P3.2** The pipeline is the product: make its state readable at a glance. What stage a
      post is in, what is blocked and why, what a review gate waits for, what a failure was
      and how to retry it. Make the run-trace view the thing you would actually watch.
- [ ] **P3.3** Naming and copy. Stage names, buttons and errors in the user's terms. No raw
      enum values, no stack traces, no `undefined` in the UI.
- [ ] **P3.4** Speed where it is felt: layout shift, avoidable spinners on cached data, lists
      that refetch everything on a single-row change.
- [ ] **P3.5** One screen per iteration, verified with `npx -y chrome-devtools-axi`, before and
      after PNGs committed under `docs/polish/ui/`, no new console errors.

## Done

- [ ] `cd web && pnpm exec tsc --noEmit`, `pnpm lint`, `pnpm test`, `pnpm build` all exit 0
      with zero failing tests, three runs with identical results, worker running and not.
- [ ] `pnpm test:e2e` passes.
- [ ] CI green on a real run, URL in the evidence.
- [ ] `pnpm test` leaves the working tree clean.
- [ ] The logo renders, verified by committed screenshot.
- [ ] No broken image is reachable in the UI, and a failed image reads as a failure with a
      reason and a retry.
- [ ] WordPress publishing works end to end against a real site, or every layer below the
      credential boundary is proven with real request assertions and the gap is stated.
- [ ] A user can find and test a publishing connection without being told where it is.
- [ ] `todo.md` holds no `[confirmed]` item affecting correctness, data integrity, security or
      the visible product.
- [ ] `docs/polish/SUMMARY.md` exists with an honest gap list.
